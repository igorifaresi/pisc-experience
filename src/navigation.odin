package pisc

search_above :: proc(to_check: Editor_Element, from: Editor_Element, from_idx: int) -> (found := false) {

	return
}

search_below :: proc(to_check: Editor_Element, from: Editor_Element, from_idx: int) -> (found := false) {

	return
}